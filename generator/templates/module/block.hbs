import type { Block } from 'payload';

export const {{pascalCase name}}Block: Block = {
  // Tem de ser igual ao `alias` do módulo: é a única ligação entre o CMS e o frontend.
  slug: '{{camelCase name}}',

  interfaceName: '{{pascalCase name}}Block',

  labels: {
    singular: '{{pascalCase name}}',
    plural: '{{pascalCase name}}',
  },

  fields: [
    {
      name: 'title',
      type: 'text',
      required: true,
      localized: true,
    },
  ],
};
