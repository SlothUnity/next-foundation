import type { Block } from 'payload';

export const {{pascalCase name}}Block: Block = {
  slug: '{{camelCase name}}',

  interfaceName: '{{pascalCase name}}Block',

  labels: {
    singular: '{{pascalCase name}}',
    plural: '{{pascalCase name}}',
  },

  fields: [
    {
      name: 'title',
      type: 'text',
      required: true,
      localized: true,
    },
  ],
};
